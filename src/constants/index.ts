export enum ViewType {
  SCROLL = 'scroll',
  PAGE1 = 'page1',
  PAGE12 = 'page12',
  PAGE23 = 'page23',
}

export enum BindingType {
  LEFT = 'left',
  RIGHT = 'right',  // manga
}

export enum ImageStatus {
  LOADING = 'loading',
  ERROR = 'error',
  LOADED = 'loaded',
}
